// Generated by CoffeeScript 1.9.3
(function() {
  var convertLine;

  convertLine = function(text, options) {
    var c, font, i, j, k, l, len, len1, lines, ref, ref1, ref2, ref3, ref4;
    font = require("./fonts/" + ((ref = options.font) != null ? ref : 'lines'));
    lines = (function() {
      var j, ref1, results;
      results = [];
      for (i = j = 0, ref1 = font.lineCount; 0 <= ref1 ? j < ref1 : j > ref1; i = 0 <= ref1 ? ++j : --j) {
        results.push("");
      }
      return results;
    })();
    ref1 = text.split("");
    for (j = 0, len = ref1.length; j < len; j++) {
      c = ref1[j];
      ref4 = ((ref3 = font[c]) != null ? ref3 : font.notSupportedChar).split((ref2 = font.newLineChar) != null ? ref2 : "n");
      for (i = k = 0, len1 = ref4.length; k < len1; i = ++k) {
        l = ref4[i];
        lines[i] += l;
      }
    }
    return lines = lines.join("\n");
  };

  module.exports = function(text, options) {
    var s;
    if (text == null) {
      text = "ascii";
    }
    if (options == null) {
      options = {};
    }
    return ((function() {
      var j, len, ref, results;
      ref = text.split('\n');
      results = [];
      for (j = 0, len = ref.length; j < len; j++) {
        s = ref[j];
        results.push(convertLine(s, options));
      }
      return results;
    })()).join('\n');
  };

  module.exports.fonts = ['lines'];

}).call(this);
